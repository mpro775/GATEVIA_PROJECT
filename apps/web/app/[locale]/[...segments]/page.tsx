import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import { ContentGrid, PageHero, SectionRenderer } from '@/components/content'; import { LeadForm } from '@/components/lead-form'; import { getDetail, getList, getPage, safe } from '@/lib/api'; import { text, translation } from '@/lib/content'; import { copy } from '@/lib/ui-copy';
const listResources=new Set(['services','industries','case-studies','insights','brands','products','clients','partners','certifications','trust-metrics','testimonials','team','faqs']);const detailResources=new Set(['services','industries','case-studies','insights','brands','products']);const formMap:Record<string,'contact'|'consultation'|'market-entry-assessment'>={contact:'contact','book-consultation':'consultation','market-entry-assessment':'market-entry-assessment'};
async function resolve(locale:string,segments:string[]){const [root,slug]=segments;if(root&&listResources.has(root)){return slug&&detailResources.has(root)?{kind:'detail' as const,resource:root,data:await safe(getDetail(root,locale,slug),{})}:{kind:'list' as const,resource:root,data:await safe(getList(root,locale),[])};}const pageSlug=segments.join('/');return{kind:'page' as const,resource:pageSlug,data:await safe(getPage(locale,pageSlug),{})}}
export async function generateMetadata({params}:{params:Promise<{locale:string;segments:string[]}>}):Promise<Metadata>{const{locale,segments}=await params;const result=await resolve(locale,segments);const entity=Array.isArray(result.data)?{}:result.data;const tr=translation(entity);const title=text(tr.seoTitle??tr.title??tr.name,segments.at(-1)?.replaceAll('-',' ')??'GATEVIA');return{title,description:text(tr.seoDescription??tr.excerpt??tr.shortDescription),alternates:{canonical:`/${locale}/${segments.join('/')}`},robots:{index:Boolean(entity.id)||result.kind==='list',follow:true}}}
export default async function DynamicPage({params}:{params:Promise<{locale:string;segments:string[]}>}){const{locale,segments}=await params;const result=await resolve(locale,segments);const pageKey=segments.join('/');if(result.kind==='list'){return <><section className="page-head"><div className="container"><span className="eyebrow">GATEVIA</span><h1>{segments[0]!.replaceAll('-',' ')}</h1></div></section><section className="section"><div className="container">{segments[0]==='insights'&&<div className="filter-bar"><input className="gv-input search-input" type="search" placeholder={copy(locale).search}/></div>}<ContentGrid items={result.data} locale={locale} resource={result.resource}/></div></section></>};const entity=result.data;if(!entity.id&&!formMap[pageKey])notFound();const tr=translation(entity);return <><PageHero translation={tr.title||tr.name?tr:{title:pageKey.replaceAll('-',' ')}} locale={locale}/>{result.kind==='page'&&<SectionRenderer sections={entity.sections} locale={locale}/>} {formMap[pageKey]&&<section className="section"><div className="container"><LeadForm kind={formMap[pageKey]!} locale={locale}/></div></section>}{result.kind==='detail'&&<section className="section"><div className="container rich-text"><p>{text(tr.overview??tr.content??tr.shortDescription??tr.excerpt)}</p></div></section>}</>}
