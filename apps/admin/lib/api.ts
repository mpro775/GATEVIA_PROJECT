import { ApiError } from '@gatevia/api-client';
const API=process.env.NEXT_PUBLIC_API_URL??'http://localhost:3002/api/v1';
function cookie(name:string){return document.cookie.split('; ').find(item=>item.startsWith(`${name}=`))?.split('=').slice(1).join('=')}
export async function api<T>(path:string,init:RequestInit={}):Promise<T>{const response=await fetch(`${API}${path}`,{...init,credentials:'include',headers:{Accept:'application/json',...(init.body?{'Content-Type':'application/json'}:{}),...(!['GET','HEAD'].includes(init.method??'GET')?{'X-CSRF-Token':cookie('gatevia_csrf')??''}:{}),...init.headers}});const body=await response.json() as {data:T;detail?:string;status?:number};if(!response.ok)throw new ApiError({type:'request-error',title:'Request failed',status:body.status??response.status,detail:body.detail??'The request failed.'});return body.data}
export const apiEnvelope=async<T>(path:string):Promise<{data:T[];meta:{page:number;pageSize:number;total:number;pageCount:number}}>{const response=await fetch(`${API}${path}`,{credentials:'include',headers:{Accept:'application/json'}});const body=await response.json() as {data:T[];meta?:{page:number;pageSize:number;total:number;pageCount:number};detail?:string};if(!response.ok)throw new Error(body.detail??'Request failed');return{data:body.data,meta:body.meta??{page:1,pageSize:body.data.length,total:body.data.length,pageCount:1}}};
