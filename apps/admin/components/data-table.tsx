'use client'; import { useEffect, useMemo, useState } from 'react'; import Link from 'next/link'; import { Badge, Button, EmptyState, ErrorState, Pagination, Skeleton } from '@gatevia/ui'; import { apiEnvelope } from '@/lib/api';
interface Props{title:string;resource:string;basePath:string;kind?:'content'|'lead'|'audit'|'language'|'user'|'role'}
const display=(row:Record<string,unknown>)=>{const tr=Array.isArray(row.translations)?row.translations[0] as Record<string,unknown>|undefined:undefined;return String(tr?.title??tr?.name??tr?.question??row.fullName??row.displayName??row.email??row.key??row.action??row.originalFilename??'Untitled')};
export function DataTable({title,resource,basePath,kind='content'}:Props){const[page,setPage]=useState(1);const[q,setQ]=useState('');const[state,setState]=useState<{rows:Record<string,unknown>[];pageCount:number}|null>(null);const[error,setError]=useState(false);const query=useMemo(()=>`?page=${page}&pageSize=20${q?`&q=${encodeURIComponent(q)}`:''}`,[page,q]);useEffect(()=>{setState(null);setError(false);void apiEnvelope<Record<string,unknown>>(`/admin/${resource}${query}`).then(result=>setState({rows:result.data,pageCount:result.meta.pageCount})).catch(()=>setError(true))},[resource,query]);return <><div className="page-title"><div><h1>{title}</h1><p>Search, filter and manage records with server-side pagination.</p></div>{kind==='content'&&<Link className="gv-button" href={`${basePath}/new`}>Create record</Link>}</div><div className="toolbar"><input className="gv-input search-input" type="search" value={q} onChange={event=>{setQ(event.target.value);setPage(1)}} placeholder="Search"/><select className="gv-input" aria-label="Status filter" defaultValue=""><option value="">All statuses</option><option>draft</option><option>published</option><option>archived</option></select></div>{error?<ErrorState title="Unable to load records" description="Check your connection or access permissions."/>:!state?<div className="panel"><Skeleton/><br/><Skeleton width="70%"/></div>:state.rows.length===0?<EmptyState title="No records yet" description="Create the first approved record when content is ready."/>:<><div className="table-wrap"><table><thead><tr><th>Name</th><th>Status / Type</th><th>Translations</th><th>Updated</th><th/></tr></thead><tbody>{state.rows.map(row=><tr key={String(row.id)}><td><div className="cell-main">{display(row)}</div><div className="cell-meta">{String(row.email??row.entityType??row.id)}</div></td><td><Badge tone={row.status==='published'||row.status==='active'?'success':row.status==='archived'||row.status==='suspended'?'danger':'neutral'}>{String(row.status??row.sourceType??row.action??'active')}</Badge></td><td>{Array.isArray(row.translations)?row.translations.length:'—'}</td><td>{row.updatedAt||row.createdAt?new Intl.DateTimeFormat('en',{dateStyle:'medium'}).format(new Date(String(row.updatedAt??row.createdAt))):'—'}</td><td><Link className="text-link" href={`${basePath}/${String(row.id)}`}>{kind==='audit'?'View':'Open'}</Link></td></tr>)}</tbody></table></div><Pagination page={page} pageCount={state.pageCount||1} onPage={setPage}/></>}</>}
