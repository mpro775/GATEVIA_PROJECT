'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { Button, Field, Input } from '@gatevia/ui'; import { api } from '@/lib/api';
export function LoginForm(){const router=useRouter();const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError('');const data=new FormData(event.currentTarget);try{await api('/auth/login',{method:'POST',body:JSON.stringify({email:data.get('email'),password:data.get('password')})});router.replace('/dashboard');router.refresh()}catch{setError('The email or password is incorrect.')}finally{setBusy(false)}}return <form onSubmit={submit}><div><span className="eyebrow">SECURE ADMINISTRATION</span><h1>Welcome back</h1><p>Sign in to manage GATEVIA content and enquiries.</p></div><Field label="Email"><Input name="email" type="email" autoComplete="username" required/></Field><Field label="Password"><Input name="password" type="password" autoComplete="current-password" minLength={12} required/></Field>{error&&<div className="form-status form-status--error" role="alert">{error}</div>}<Button disabled={busy}>{busy?'Signing in…':'Sign in'}</Button></form>}
