'use client'; import { useEffect, useState } from 'react'; import { Card, ErrorState, Skeleton } from '@gatevia/ui'; import { apiEnvelope } from '@/lib/api';
export function Dashboard(){const[rows,setRows]=useState<Record<string,unknown>[]|null>(null);const[error,setError]=useState(false);useEffect(()=>{void apiEnvelope<Record<string,unknown>>('/admin/leads?pageSize=100').then(result=>setRows(result.data)).catch(()=>setError(true))},[]);if(error)return <ErrorState title="Dashboard unavailable" description="Check the API connection and your permissions."/>;const data=rows??[];const count=(status:string)=>data.filter(item=>item.status===status).length;return <><div className="page-title"><div><h1>Dashboard</h1><p>Current lead and content operations.</p></div></div><div className="metric-grid">{[['Total leads',rows?data.length:null],['New leads',rows?count('new'):null],['Qualified',rows?count('qualified'):null],['Proposals',rows?count('proposal'):null]].map(([label,value])=><Card className="metric" key={String(label)}><span>{label}</span>{value===null?<Skeleton width="50%"/>:<strong>{value}</strong>}</Card>)}</div></>}
