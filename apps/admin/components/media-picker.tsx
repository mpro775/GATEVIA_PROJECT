'use client'; import { useEffect, useRef, useState } from 'react'; import { Button, EmptyState, Input } from '@gatevia/ui'; import { apiEnvelope } from '@/lib/api';
interface MediaRow{id:string;originalFilename:string;status:string;translations:Array<{altText?:string}>}
export function MediaPicker({value,onSelect}:{value?:string;onSelect:(id:string)=>void}){const dialog=useRef<HTMLDialogElement>(null);const[rows,setRows]=useState<MediaRow[]>([]);const[q,setQ]=useState('');useEffect(()=>{if(dialog.current?.open)void apiEnvelope<MediaRow>(`/admin/media?pageSize=100&status=ready&q=${encodeURIComponent(q)}`).then(result=>setRows(result.data))},[q]);function open(){dialog.current?.showModal();void apiEnvelope<MediaRow>('/admin/media?pageSize=100&status=ready').then(result=>setRows(result.data))}return <div><div className="toolbar"><Input readOnly value={value??''} placeholder="No media selected"/><Button type="button" onClick={open}>Choose media</Button></div><dialog ref={dialog} className="panel"><div className="page-title"><h2>Media picker</h2><button className="text-link" onClick={()=>dialog.current?.close()}>Close</button></div><Input type="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search media"/>{rows.length?<div className="media-grid">{rows.map(row=><button className="media-tile" key={row.id} onClick={()=>{onSelect(row.id);dialog.current?.close()}}><strong>{row.originalFilename}</strong><span className="cell-meta">{row.translations[0]?.altText??row.status}</span></button>)}</div>:<EmptyState title="No ready media found" description="Upload an approved asset in the Media Library first."/>}</dialog></div>}
