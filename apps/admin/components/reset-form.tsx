'use client'; import { useState } from 'react'; import { useRouter, useSearchParams } from 'next/navigation'; import { Button, Field, Input } from '@gatevia/ui'; import { api } from '@/lib/api';
export function ResetForm({invitation=false}:{invitation?:boolean}){const query=useSearchParams();const router=useRouter();const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const data=new FormData(event.currentTarget);if(data.get('password')!==data.get('confirm')){setMessage('Passwords do not match.');setBusy(false);return}try{await api('/auth/reset-password',{method:'POST',body:JSON.stringify({token:query.get('token'),password:data.get('password')})});router.replace('/login')}catch{setMessage('This one-time link is invalid or expired.')}finally{setBusy(false)}}return <form onSubmit={submit}><div><span className="eyebrow">GATEVIA ADMIN</span><h1>{invitation?'Activate your account':'Reset password'}</h1></div><Field label="New password" hint="Use at least 16 characters."><Input type="password" name="password" minLength={16} required autoComplete="new-password"/></Field><Field label="Confirm password"><Input type="password" name="confirm" minLength={16} required autoComplete="new-password"/></Field>{message&&<div className="form-status form-status--error" role="alert">{message}</div>}<Button disabled={busy}>{busy?'Saving…':'Set password'}</Button></form>}
